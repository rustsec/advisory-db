```toml
[advisory]
id = "RUSTSEC-0000-0000"
package = "cosmian_kyber"
date = "2026-08-16"
url = "https://github.com/Cosmian/kyber/pull/5"
categories = ["crypto-failure"]
keywords = ["kyber", "ml-kem", "key-recovery", "fujisaki-okamoto", "ind-cca", "avx2"]
aliases = []   # TODO: add CVE (MITRE request) + any GHSA once assigned
cvss = "CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N"

[affected]
# Reachable only when built with the (non-default, opt-in) `avx2` feature on an
# x86_64 target, via the public `decapsulate` function. The default/reference
# backend is not affected. cargo-audit cannot see feature flags, so this advisory
# flags all affected-version builds; the avx2-only scope is in the body.

[versions]
# cosmian_kyber is a stale fork of Argyle-Software/kyber and is unmaintained; no
# fixed release exists.
patched = []
```

# cosmian_kyber: AVX2 backend skips Fujisaki-Okamoto implicit rejection, enabling chosen-ciphertext key recovery

## Summary

`cosmian_kyber` is a fork of `Argyle-Software/kyber` and inherits a broken
constant-time conditional-move routine `cmov` in its AVX2 backend. When built
with the opt-in `avx2` feature on x86_64, `cmov` is a no-op: it never performs
the copy. That `cmov` is the step that applies the Fujisaki-Okamoto (FO) implicit
rejection during decapsulation. With it disabled, decapsulating an *invalid*
ciphertext no longer returns a key-independent pseudorandom value; it returns a
value that depends on the decrypted plaintext of the attacker-chosen ciphertext.
This restores the chosen-ciphertext decryption oracle that the FO transform
exists to remove, and yields full recovery of the static secret key.

The attack requires no timing measurement, no side channel, and no faults; only
the ability to submit ciphertexts to a decapsulation operation under a reused
key pair and observe the resulting shared secret.

## Affected configuration

Reachable only in builds with `features = ["avx2"]` on an x86_64 target. The
default (reference) backend and all non-x86_64 targets are unaffected.

## Root cause

`src/avx2/verify.rs`: the AVX2 `cmov` builds its blend mask with
`_mm256_set1_epi64x(b as i64)`, so for `b == 1` each lane is
`0x00000000_00000001`, every byte's high bit is `0`, and `_mm256_blendv_epi8`
copies nothing. The vector loop is a no-op for any `b`. The correct form, as in
the pq-crystals reference AVX2 code, negates the condition byte and broadcasts
per byte with `_mm256_set1_epi8`.

## Reachability

Decapsulation (`crypto_kem_dec`) calls `cmov` with `len == KYBER_SYMBYTES == 32`,
which runs entirely through the broken 32-byte vector loop. On an invalid
ciphertext the pre-key `G(m' ‖ h(pk))[0..32]` is not overwritten with the
rejection secret, so the returned shared secret is
`KDF(G(m' ‖ h(pk))[0..32] ‖ H(ct))` where `m' = Decrypt(ct)`. Every input except
`m'` is public, giving a plaintext-checking oracle.

## Impact

Complete IND-CCA break. The plaintext-checking oracle drives a standard
chosen-ciphertext key-recovery attack and extracts the entire static secret key.
Any protocol reusing a Kyber key pair across decapsulations is exposed;
ephemeral-only key shares are not. The identical defect in the parent crate was
verified end to end on ML-KEM-768 (full key recovered in 4,272 decapsulation
queries).

## Maintenance status

`cosmian_kyber` is a stale fork (last published 0.1.0, January 2023) of an
unmaintained upstream, and no fixed release is expected.

## Mitigation

- Migrate to a maintained implementation such as `ml-kem`.
- If remaining on this crate, do not enable the `avx2` feature; the default
  reference backend performs implicit rejection correctly.
- Do not reuse a Kyber key pair across decapsulations.

## Credit

Reported by 007bsd.
