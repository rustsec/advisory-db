```toml
[advisory]
id = "RUSTSEC-0000-0000"
package = "sevenz_rust"
date = "2026-08-06"
informational = "unmaintained"
categories = ["file-disclosure"]
cvss = CVSS:4.0/AV:L/AC:L/AT:N/PR:N/UI:A/VC:N/VI:H/VA:L/SC:H/SI:H/SA:H

[versions]
patched = []
```
# Relative Path Traversal (CWE-23) in `decompress_impl` that enables an arbitrary file write.

All versions of `sevenz_rust` fail to verify file paths when extracting in `decompress_impl` (the underlying function used in the exposed decompress api functions). This allows a maliciously crafted archive that has paths outside the extraction directory, both relative paths such as `../../../...` or absolute paths like `C:\\...` or `/etc/...`, which could lead to file disclosure or remote command execution in combination with other flaws or social engineering.

The project has been abandoned and the repository has been deleted, so no upstream is expected. Users should migrate away from the library in favor of a modern up-to-date 7z library such as `sevenz_rust2`.